using UnityEngine;
using System.Collections;

public class TagGameManager : MonoBehaviour
{
//    [SerializeField] private TagRoleManager roleManager;
//    [SerializeField] private float tagDistance = 2f;

//    private void Update()
//    {
//        if (Vector3.Distance(GetTransform(roleManager.currentTagger), GetTransform(roleManager.currentRunner)) < tagDistance)
//        {
//            roleManager.currentRunner.OnTagged();
//        }
//    }

//    private Transform GetTransform(ITaggable taggable)
//    {
//        return ((MonoBehaviour)taggable).transform;
//    }
}
